import {PanelShell} from '@/components/PanelShell';import {requirePartner} from '@/lib/partner-auth';
export const dynamic='force-dynamic';export default async function Page(){const auth=await requirePartner();return <PanelShell kind="mitra"><h1>Pengaturan</h1><div className="card detail-card"><p><b>Akun:</b> {auth.user.phone||auth.user.email||auth.user.username||auth.user.id}</p><p><b>Toko:</b> {auth.partner.businessName}</p><p><b>Claim:</b> {auth.partner.claimStatus}</p><p><b>Storefront:</b> {auth.partner.storefrontStatus}</p><p className="muted">Perubahan data sensitif seperti akun pembayaran tidak dilakukan otomatis oleh Agent Mitra.</p></div></PanelShell>}
