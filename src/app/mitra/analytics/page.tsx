import {PanelShell} from '@/components/PanelShell';
import {requirePartner} from '@/lib/partner-auth';
import {partnerAnalytics} from '@/lib/partner-operations';
import {rupiah} from '@/lib/money';
export const dynamic='force-dynamic';
export default async function Page({searchParams}:{searchParams:Promise<{period?:string}>}){const auth=await requirePartner(),sp=await searchParams,period=['today','7','30'].includes(sp.period||'')?sp.period!:'30',since=period==='today'?new Date(new Date().setHours(0,0,0,0)):new Date(Date.now()-Number(period)*86400000),a=await partnerAnalytics(auth.partner.id,since);const metrics:[string,string|number][]=[['Profile Views',a.profileViews],['Service Views',a.serviceViews],['Offer Received',a.offersReceived],['Offer Opened',a.offersOpened],['Orders Received',a.ordersReceived],['Orders Accepted',a.ordersAccepted],['Orders Rejected',a.ordersRejected],['Completed Orders',a.completedOrders],['Cancelled Orders',a.cancelledOrders],['Conversion Rate',a.conversionRate==null?'—':`${Math.round(a.conversionRate*100)}%`],['Acceptance Rate',a.acceptanceRate==null?'—':`${Math.round(a.acceptanceRate*100)}%`],['Average Response Time',a.averageResponseMinutes==null?'—':`${a.averageResponseMinutes} menit`],['Revenue',rupiah(a.revenue)],['Rating',a.rating==null?'—':a.rating.toFixed(2)],['Top Service',a.topService?`${a.topService.name} (${a.topService.quantity})`:'—']];return <PanelShell kind="mitra"><div className="order-head"><div><h1>Statistik Toko</h1><p className="muted">Data hanya untuk toko Anda sendiri.</p></div><div className="split-actions"><a className="btn outline small" href="?period=today">Hari Ini</a><a className="btn outline small" href="?period=7">7 Hari</a><a className="btn outline small" href="?period=30">30 Hari</a></div></div><div className="grid metric-grid">{metrics.map(([k,v])=><div className="card metric" key={k}><small className="muted">{k}</small><b>{String(v)}</b></div>)}</div></PanelShell>}
