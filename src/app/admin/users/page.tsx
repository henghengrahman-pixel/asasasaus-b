import {PanelShell} from '@/components/PanelShell';import {PageHeader,StatusBadge,EmptyState} from '@/components/AdminBits';import {db} from '@/lib/db';import {requireAdmin} from '@/lib/auth';
export const dynamic='force-dynamic';export default async function Page(){const me=await requireAdmin(['SUPER_ADMIN']);const rows=await db.user.findMany({where:{role:{in:['SUPER_ADMIN','OPERATIONS','CS','FINANCE','PARTNER_ADMIN']}},orderBy:{createdAt:'asc'}});return <PanelShell kind="admin"><PageHeader title="Users & Roles" description="Admin identities, role, 2FA, session revocation, dan account state."/><section className="admin-card"><h2 className="section-title">Tambah Admin</h2><form method="post" action="/api/admin/action" className="form-grid"><input type="hidden" name="action" value="admin.create"/><label className="field-label">Display name<input name="displayName"/></label><label className="field-label">Username<input name="username" required/></label><label className="field-label">Password awal<input name="password" type="password" minLength={12} required/></label><label className="field-label">Role<select name="role"><option>OPERATIONS</option><option>CS</option><option>FINANCE</option><option>PARTNER_ADMIN</option><option>SUPER_ADMIN</option></select></label><div className="full"><button className="btn primary">Tambah Admin</button></div></form></section><div className="stack" style={{marginTop:16}}>{rows.map(x=><section className="admin-card" key={x.id}><div className="page-header"><div><b>{x.displayName||x.username}</b><p className="page-description">@{x.username} • last login {x.lastLoginAt?.toLocaleString('id-ID')||'-'}</p></div><div className="toolbar"><StatusBadge status={x.active?'ACTIVE':'INACTIVE'}/><StatusBadge status={x.totpEnabled?'2FA_ON':'2FA_OFF'}/></div></div><div className="split-actions"><form method="post" action="/api/admin/action" className="input-group"><input type="hidden" name="action" value="admin.role"/><input type="hidden" name="id" value={x.id}/><select name="role" defaultValue={x.role}><option>SUPER_ADMIN</option><option>OPERATIONS</option><option>CS</option><option>FINANCE</option><option>PARTNER_ADMIN</option></select><button className="btn outline small">Change Role</button></form><form method="post" action="/api/admin/action"><input type="hidden" name="action" value="admin.revokeSessions"/><input type="hidden" name="id" value={x.id}/><button className="btn outline small">Revoke Sessions</button></form><form method="post" action="/api/admin/action"><input type="hidden" name="action" value="admin.reset2fa"/><input type="hidden" name="id" value={x.id}/><button className="btn warning small">Reset 2FA</button></form><form method="post" action="/api/admin/action" className="input-group"><input type="hidden" name="action" value="admin.resetPassword"/><input type="hidden" name="id" value={x.id}/><input name="password" type="password" minLength={12} placeholder="Password baru" required/><button className="btn warning small">Reset Password</button></form>{x.id!==me.user.id&&<form method="post" action="/api/admin/action"><input type="hidden" name="action" value="admin.toggle"/><input type="hidden" name="id" value={x.id}/><button className="btn danger small">{x.active?'Disable':'Enable'}</button></form>}</div></section>)}{!rows.length&&<EmptyState title="Belum ada admin"/>}</div></PanelShell>}
