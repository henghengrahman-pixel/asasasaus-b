import {CheckCircle2,XCircle} from 'lucide-react';import {PanelShell} from '@/components/PanelShell';import {PageHeader} from '@/components/AdminBits';import {db} from '@/lib/db';import {requireAdmin} from '@/lib/auth';import {safeJsonRecord,str} from '@/lib/admin-ui';import {storageConfig} from '@/lib/storage';
export const dynamic='force-dynamic';export default async function Page(){await requireAdmin(['SUPER_ADMIN']);const [s,ranking]=await Promise.all([db.setting.findUnique({where:{key:'business'}}),db.setting.findUnique({where:{key:'partnerRanking'}})]),v=safeJsonRecord(s?.value),rv=safeJsonRecord(ranking?.value),storage=storageConfig();return <PanelShell kind="admin"><PageHeader title="Settings" description="Business, order, maps/storage indicators, security dan SEO."/><form method="post" action="/api/admin/action" className="stack"><input type="hidden" name="action" value="settings.save"/><section className="admin-card"><h2 className="section-title">General</h2><div className="form-grid"><label className="field-label">Business name<input name="businessName" defaultValue={str(v.businessName,'JasaBatam')} required/></label><label className="field-label">WhatsApp<input name="whatsapp" defaultValue={str(v.whatsapp,'')}/></label><label className="field-label">Support email<input name="supportEmail" type="email" defaultValue={str(v.supportEmail,'')}/></label><label className="field-label">Timezone<input name="timezone" defaultValue={str(v.timezone,'Asia/Jakarta')} required/></label><label className="field-label">Currency<input name="currency" defaultValue={str(v.currency,'IDR')} required/></label></div></section><section className="admin-card"><h2 className="section-title">Order</h2><div className="form-grid"><label className="field-label">Default commission (bps)<input name="defaultCommissionBps" type="number" min="0" max="10000" defaultValue={Number(v.defaultCommissionBps||1000)}/></label><label className="field-label">Min scheduling lead (minutes)<input name="minSchedulingLeadMinutes" type="number" min="0" defaultValue={Number(v.minSchedulingLeadMinutes||60)}/></label><label className="field-label">Cancellation window (minutes)<input name="cancellationWindowMinutes" type="number" min="0" defaultValue={Number(v.cancellationWindowMinutes||120)}/></label></div></section><div className="admin-grid-2"><section className="admin-card"><h2 className="section-title">Maps</h2><p><CheckCircle2 size={16}/> OpenStreetMap aktif</p><p className="page-description">Peta dasar tidak memerlukan Google Maps API key. Koordinat order/mitra tetap disimpan di database.</p></section><section className="admin-card"><h2 className="section-title">Storage</h2><p>{storage.configured?<><CheckCircle2 size={16}/> S3 configured</>:<><XCircle size={16}/> S3 not configured</>}</p><p className="page-description">Bucket: {storage.bucket?'configured':'missing'} • public URL: {storage.publicBase?'configured':'missing'}. Credentials disembunyikan.</p></section></div><section className="admin-card"><h2 className="section-title">Partner Ranking</h2><p className="page-description">Threshold minimum disimpan terpusat di Settings dan digunakan scoring service server-side.</p><div className="form-grid"><label className="field-label">Minimum completed orders<input name="minimumCompletedOrders" type="number" min="1" max="10000" defaultValue={Number(rv.minimumCompletedOrders||5)}/></label><label className="field-label">Minimum rating count<input name="minimumRatingCount" type="number" min="1" max="10000" defaultValue={Number(rv.minimumRatingCount||3)}/></label></div></section><section className="admin-card"><h2 className="section-title">Security</h2><div className="kpi-list"><div className="kpi-row"><span>Admin session</span><b>12 jam • HttpOnly • SameSite=Lax • Secure production</b></div><div className="kpi-row"><span>2FA</span><b>TOTP enrollment pada login pertama</b></div><div className="kpi-row"><span>Password</span><b>Minimal 12 karakter untuk admin baru/reset</b></div></div></section><section className="admin-card"><h2 className="section-title">SEO</h2><div className="form-grid"><label className="field-label">Site title<input name="siteTitle" defaultValue={str(v.siteTitle,'JasaBatam — Semua Jasa di Batam, Lebih Mudah')}/></label><label className="field-label">Site description<input name="siteDescription" defaultValue={str(v.siteDescription,'Marketplace jasa lokal Batam.')}/></label></div></section><div><button className="btn primary">Simpan Settings</button></div></form></PanelShell>}
