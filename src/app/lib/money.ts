export const rupiah=(n:number)=>new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0}).format(n); export function calculatePrice(base:number,qty:number,addons:number[],discount=0){if(!Number.isSafeInteger(base)||!Number.isSafeInteger(qty)||qty<1)throw new Error('Invalid price');const subtotal=base*qty+addons.reduce((a,b)=>a+b,0);return{subtotal,discount:Math.min(Math.max(discount,0),subtotal),total:subtotal-Math.min(Math.max(discount,0),subtotal)}} export const commission=(total:number,bps:number)=>Math.floor(total*bps/10000);