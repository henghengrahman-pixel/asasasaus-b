import Link from 'next/link';import {ChevronLeft,ChevronRight} from 'lucide-react';import {qs,statusTone} from '@/lib/admin-ui';
export function PageHeader({title,description,children}:{title:string;description?:string;children?:React.ReactNode}){return <div className="page-header"><div><h1 className="page-title">{title}</h1>{description&&<p className="page-description">{description}</p>}</div>{children&&<div className="toolbar">{children}</div>}</div>}
export function StatusBadge({status}:{status:string}){return <span className={'status-badge '+statusTone(status)}>{status.replaceAll('_',' ')}</span>}
export function Pagination({path,page,pageSize,total,params={}}:{path:string;page:number;pageSize:number;total:number;params?:Record<string,string|number|undefined>}){const pages=Math.max(1,Math.ceil(total/pageSize));return <div className="pagination"><span>Halaman {page} dari {pages} • {total} data</span><div className="pagination-actions">{page>1?<Link href={qs(path,{...params,page:page-1,pageSize})}><ChevronLeft size={14}/> Sebelumnya</Link>:<span>Sebelumnya</span>}{page<pages?<Link href={qs(path,{...params,page:page+1,pageSize})}>Berikutnya <ChevronRight size={14}/></Link>:<span>Berikutnya</span>}</div></div>}
export function EmptyState({title='Belum ada data',description}:{title?:string;description?:string}){return <div className="empty-state"><b>{title}</b>{description&&<div className="page-description">{description}</div>}</div>}
