import {describe,it,expect} from 'vitest';import {haversineKm,isListingEligible,partnerSlug,rankingScore,rankComparator,badges} from '../../src/lib/partner-storefront';
describe('partner storefront rules',()=>{it('creates stable safe slug',()=>expect(partnerSlug('HL Laundry Batam!')).toBe('hl-laundry-batam'));it('allows operational DRAFT partner without claiming verification',()=>expect(isListingEligible({status:'DRAFT',online:true,dispatchEnabled:true,storefrontStatus:'PUBLISHED',user:{active:true}})).toBe(true));it.each(['REJECTED','SUSPENDED'])('excludes %s partner',status=>expect(isListingEligible({status,online:true,dispatchEnabled:true,storefrontStatus:'PUBLISHED',user:{active:true}})).toBe(false));it('requires online and dispatch',()=>{expect(isListingEligible({status:'DRAFT',online:false,dispatchEnabled:true,storefrontStatus:'PUBLISHED',user:{active:true}})).toBe(false);expect(isListingEligible({status:'DRAFT',online:true,dispatchEnabled:false,storefrontStatus:'PUBLISHED',user:{active:true}})).toBe(false)});it('haversine returns realistic Batam short distance',()=>expect(haversineKm(1.13,104.03,1.14,104.04)).toBeGreaterThan(1));it('minimum sample blocks one perfect review',()=>expect(rankingScore({averageRating:5,ratingCount:1,completedOrders:1,completionRate:1,responseRate:1,averageResponseMinutes:1,cancellationRate:0}).eligible).toBe(false));it('tie breaker is deterministic',()=>{const a={partnerId:'a',score:.8,completedOrders:10,ratingCount:8},b={partnerId:'b',score:.8,completedOrders:10,ratingCount:8};expect([b,a].sort(rankComparator).map(x=>x.partnerId)).toEqual(['a','b'])});it('verified badge only for approved',()=>{const base={createdAt:new Date(0),ratingAvg:4.9,ratingCount:30,completedOrders:60,averageResponseMinutes:5};expect(badges({status:'DRAFT',...base})).not.toContain('Terverifikasi');expect(badges({status:'APPROVED',...base})).toContain('Terverifikasi')})});
